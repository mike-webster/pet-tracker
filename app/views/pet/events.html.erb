<div class="content">
    <h2><%= @pet.name.titleize %>'s Events</h2>
    <% unless @events.present? %>
      <p>No events yet</p>
    <% else %>
      <% @events.each do |e| %>
        <div class="row">
          <span>Event: <%= e.kind.titleize %></span>
          <span>When: <%= show_local_time(e.happened_at) %></span>
          <% unless e.notes.blank? %>
            <span>Notes: <%= e.notes %></span>
          <% end %>
        </div>
      <% end %>
    <% end %>
</div>